// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: metric_seg_cleanup.sql

package lrdb

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const metricSegmentCleanupDelete = `-- name: MetricSegmentCleanupDelete :exec
DELETE FROM metric_seg
WHERE organization_id = $1
  AND dateint = $2
  AND frequency_ms = $3
  AND segment_id = $4
  AND instance_num = $5
`

type MetricSegmentCleanupDeleteParams struct {
	OrganizationID uuid.UUID `json:"organization_id"`
	Dateint        int32     `json:"dateint"`
	FrequencyMs    int32     `json:"frequency_ms"`
	SegmentID      int64     `json:"segment_id"`
	InstanceNum    int16     `json:"instance_num"`
}

func (q *Queries) MetricSegmentCleanupDelete(ctx context.Context, arg MetricSegmentCleanupDeleteParams) error {
	_, err := q.db.Exec(ctx, metricSegmentCleanupDelete,
		arg.OrganizationID,
		arg.Dateint,
		arg.FrequencyMs,
		arg.SegmentID,
		arg.InstanceNum,
	)
	return err
}

const metricSegmentCleanupGet = `-- name: MetricSegmentCleanupGet :many
SELECT
  organization_id,
  dateint,
  frequency_ms,
  segment_id,
  instance_num,
  file_size,
  lower(ts_range)::bigint as ts_range_lower
FROM metric_seg
WHERE organization_id = $1
  AND dateint = $2
  AND published = false
  AND created_at < $3
LIMIT $4
`

type MetricSegmentCleanupGetParams struct {
	OrganizationID uuid.UUID `json:"organization_id"`
	Dateint        int32     `json:"dateint"`
	AgeThreshold   time.Time `json:"age_threshold"`
	MaxRows        int32     `json:"max_rows"`
}

type MetricSegmentCleanupGetRow struct {
	OrganizationID uuid.UUID `json:"organization_id"`
	Dateint        int32     `json:"dateint"`
	FrequencyMs    int32     `json:"frequency_ms"`
	SegmentID      int64     `json:"segment_id"`
	InstanceNum    int16     `json:"instance_num"`
	FileSize       int64     `json:"file_size"`
	TsRangeLower   int64     `json:"ts_range_lower"`
}

func (q *Queries) MetricSegmentCleanupGet(ctx context.Context, arg MetricSegmentCleanupGetParams) ([]MetricSegmentCleanupGetRow, error) {
	rows, err := q.db.Query(ctx, metricSegmentCleanupGet,
		arg.OrganizationID,
		arg.Dateint,
		arg.AgeThreshold,
		arg.MaxRows,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []MetricSegmentCleanupGetRow
	for rows.Next() {
		var i MetricSegmentCleanupGetRow
		if err := rows.Scan(
			&i.OrganizationID,
			&i.Dateint,
			&i.FrequencyMs,
			&i.SegmentID,
			&i.InstanceNum,
			&i.FileSize,
			&i.TsRangeLower,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
