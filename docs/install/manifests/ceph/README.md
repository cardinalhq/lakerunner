# Ceph Object Storage

This is an example of using Ceph to provide an S3 compatible
object storage bucket for use by Lakerunner.

If you chose a different namespace than the default, you
will need to alter these manifests to point to the correct
pubsub service.
